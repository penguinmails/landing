---
import TermsOfServicePage from "@/components/pages/legal/TermsOfServicePage.astro";
import Layout from "@/layouts/Layout.astro";
import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import { ui } from "@/i18n/ui";

const { locale } = Astro.params;

if (locale && !(locale in ui)) {
  return Astro.redirect("/404");
}

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={`${t("legal.terms.title")} | ${t("site.title")}`} description={t("legal.terms.description")}>
  <main class="bg-white">
    <TermsOfServicePage t={t} />
  </main>
</Layout>
