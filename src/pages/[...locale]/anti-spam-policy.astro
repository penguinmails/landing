---
import AntiSpamPolicyPage from "@/components/pages/legal/AntiSpamPolicyPage.astro";
import Layout from "@/layouts/Layout.astro";
import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import { ui } from "@/i18n/ui";

const { locale } = Astro.params;

if (locale && !(locale in ui)) {
  return Astro.redirect("/404");
}

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={`${t("legal.antiSpam.title")} | ${t("site.title")}`} description={t("legal.antiSpam.description")}>
  <main class="bg-white">
    <AntiSpamPolicyPage t={t} />
  </main>
</Layout>
