---
import Button from "../ui/Button.astro";
---

<section
  class="w-full bg-linear-to-r from-[#1f5fe0] via-primary to-[#1374f5] py-16 sm:py-20"
  id="final-cta"
>
  <div class="mx-auto w-full max-w-5xl px-4 sm:px-6 lg:px-8">
    <div class="mx-auto flex max-w-3xl flex-col items-center text-center">
      <h2
        class="text-balance text-4xl font-semibold leading-tight text-white sm:text-5xl md:text-6xl"
      >
        Ready to Send Emails That Actually Land?
      </h2>

      <p class="mt-4 text-xl text-white/95 sm:text-2xl">
        Stop guessing. Start delivering.
      </p>

      <div
        class="mt-9 flex w-full max-w-xl flex-col items-center justify-center gap-4 sm:flex-row"
      >
        <Button
          href="/signup"
          className="min-h-14 w-full rounded-full border border-transparent bg-white px-8 text-xl font-semibold text-primary hover:bg-slate-100 sm:w-auto sm:min-w-56"
        >
          Start Free Trial
        </Button>
        <Button
          href="/demo"
          className="min-h-14 w-full rounded-full border border-white/70 bg-transparent px-8 text-xl font-semibold text-white hover:bg-white/10 sm:w-auto sm:min-w-56"
        >
          Book a Demo
        </Button>
      </div>

      <p class="mt-6 text-sm text-white/90 sm:text-lg">
        No credit card required. 14-day free trial.
      </p>
    </div>
  </div>
</section>
